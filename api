// api/index.js ou api/[nom].js
import express from 'express';
import { createServer } from '@vercel/node';

const app = express();
app.use(express.json());

app.post('/api/auth/login', (req, res) => {
  res.json({ message: 'Login OK' });
});

export default createServer(app);
